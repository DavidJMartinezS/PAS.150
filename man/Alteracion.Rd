% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alteracion.R
\name{Alteracion}
\alias{Alteracion}
\alias{get_alter_bnp_remanente}
\alias{get_alter_censo_buffer}
\title{Identifica y calcula la superficie de alteración en fragmentos remanentes de BNP}
\usage{
get_alter_bnp_remanente(BNP, obras, sp, sup_ha_max = 0.5)

get_alter_censo_buffer(
  BNP,
  obras,
  censos,
  sp,
  distancia = 5,
  buffer = 5,
  dTolerance = 2,
  radio = 1
)
}
\arguments{
\item{BNP}{Objeto `sf` (polígonos) que representa el Bosque Nativo de Preservación.
Debe contener al menos las columnas `Nom_ssubc` y `BNP_ECC`.}

\item{obras}{Objeto `sf` (polígonos) que representa las áreas de intervención de las obras.}

\item{sp}{String con el nombre científico de la especie objeto de protección (ECC)
para filtrar en la columna `BNP_ECC`.}

\item{sup_ha_max}{Valor numérico que define el umbral máximo de superficie (en hectáreas)
para que un fragmento seccionado sea considerado "alterado". Por defecto es 0.5 ha. (Solo para `get_alter_bnp_remanente`).}

\item{censos}{Objeto `sf` (puntos) con los registros de censo. (Solo para `get_alter_censo_buffer`).}

\item{distancia}{Valor numérico. Distancia máxima (en metros) desde un individuo censado hasta una obra para ser considerado en la zona de alteración por buffer. (Solo para `get_alter_censo_buffer`).}

\item{buffer}{Valor numérico. Radio del buffer (en metros) a aplicar a los individuos censados cercanos a las obras. (Solo para `get_alter_censo_buffer`).}

\item{dTolerance}{Valor numérico. Número de decimales para redondear las distancias calculadas. (Solo para `get_alter_censo_buffer`).}

\item{radio}{Valor numérico entre 0 y 1. Parámetro para controlar la concavidad del casco cóncavo (concave hull) generado alrededor de los puntos agrupados. Un valor de 1 produce un casco convexo, mientras que valores menores permiten formas más cóncavas. (Solo para `get_alter_censo_buffer`).}
}
\value{
Un objeto `sf` con los polígonos de BNP remanente que cumplen con el criterio
  de tamaño máximo, etiquetados con la columna `Afectacion`. Si no se encuentran
  remanentes que cumplan los criterios, devuelve `NULL` de forma invisible.
}
\description{
Esta función procesa la capa de Bosque Nativo de Preservación (BNP) para identificar
fragmentos que, tras ser seccionados por las obras del proyecto (intervención),
resultan en superficies remanentes menores a un umbral específico. Estos fragmentos
se clasifican como "Alteración BNP remanente" debido a que su pequeño tamaño
compromete su viabilidad biológica o conectividad.
}
\examples{
\dontrun{
  alter_1 <- get_alter_bnp_remanente(
    BNP = sf::read_sf("ruta/capa/BNP.shp"),
    obras = sf::read_sf("ruta/capa/Obras.shp"),
    sp = "Porlieria chilensis",
    sup_ha_max = 0.5
  )
  alter_2 <- get_alter_bnp_remanente(
    BNP = sf::read_sf("ruta/capa/BNP.shp"),
    obras = sf::read_sf("ruta/capa/Obras.shp"),
    censos = sf::read_sf("ruta/capa/Censos.shp"),
    sp = "Porlieria chilensis"
  )
}

}
